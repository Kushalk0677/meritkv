# Downloaded Native-vLLM Results

The two ZIP files are the originally downloaded result archives. Their contents
are expanded into the four `attempt_*` directories in this folder. The
timestamped archive is an earlier capture; the generic archive is the final
download produced by the Colab wrapper. Both are retained for byte-level
provenance and included in the archive checksum inventory.
