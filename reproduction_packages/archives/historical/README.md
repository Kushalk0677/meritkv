# Historical Transfer Archives

These archives were retained in P100 and incoming handoff directories. They
were renamed descriptively during release cleanup; their bytes were not
changed. Expanded trees are in `../../historical_transfers/`.

Archive digests are recorded in `../SHA256SUMS.txt` and the repository release
inventory. Paired ZIP/TGZ packages are retained when both transfer formats were
present.
