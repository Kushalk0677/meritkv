# Legacy Gemma-4 E2B Validation Package

This is the retained Gemma-4 E2B-only predecessor of the combined five-model
exact-splice validation package. It is preserved because its notebook and
summary utility are unique. Use `../exact_splice_validation/` for the current
multi-model validation workflow.
