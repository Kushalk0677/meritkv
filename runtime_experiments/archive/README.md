# Historical Runtime Snapshots

`root_workspace_snapshot_202608/` preserves the older Gemma-4 runtime tree that
remained at the research workspace root. It contains 90 small aggregate and raw
records with values distinct from the later V10 campaign.

The current paper-facing runtime evidence remains in `../gemma4/`, `../qwen2.5/`,
and `../native_enforcement_blackwell/`.
